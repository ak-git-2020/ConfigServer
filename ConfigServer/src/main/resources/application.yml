#Configuration file
server:
    port: 9098

Spring:
  application:
    name: CONFIG-SERVER

  cloud:
    config:
        server:
          git:
            uri: https://github.com/soft-aware/ms-config-service
            clone-on-start: true



